npm i gh-pages = git으로 업로드
package.json 에서 맨아래에 "homepage" : 를 사용하여 깃 정보등록
"homepage" : "https://{github_username}.github.io/{레파지토리 이름}"
(git remote -v 로 레파지토리 확인가능)
"scripts" 에
"deploy" : "gh-pages -d build"
 추가 (ghpages를 실행시키고 build라는 디렉토리를 가져감)

"predeploy" : "npm run build" 을 추가하여
deploy를 실행할때 node에서 predeploy 를 먼저 실행 시킴 

터미널에 npm run deploy로 실행